"""
Stałe fizyczne i parametry drona - UKŁAD NED (Z w dół)
"""

import numpy as np

# ============================================================================
# PHYSICAL CONSTANTS
# ============================================================================

DEG2RAD = np.pi / 180.0
RAD2DEG = 180.0 / np.pi

g = 9.81
RO_0 = 1.225

# ============================================================================
# DRONE PARAMETERS
# ============================================================================

MASS = 5.0
S = 1.0
CD_0 = 0.30
MOTOR_ARM_LENGTH = 0.5

# ============================================================================
# MOMENT OF INERTIA
# ============================================================================

IY = MASS * (MOTOR_ARM_LENGTH / 2.0) ** 2

# ============================================================================
# AERODYNAMIC DAMPING
# ============================================================================

CM_Q = -0.01

# ============================================================================
# MOTOR/ACTUATOR DYNAMICS
# ============================================================================

TAU = 0.05
THRUST_SCALING = 1.0

# ============================================================================
# ENVIRONMENTAL CONDITIONS
# ============================================================================

az_turbulence = 0.0
ax_wind = 0.0
az_wind = 0.0

c_turb = 1000.0
X_turb_1 = 1500.0
X_turb_2 = 2000.0

# ============================================================================
# INITIAL CONDITIONS - UKŁAD NED (Z w dół!)
# ============================================================================

h0 = 2.0        # Wysokość początkowa nad ziemią [m]
z0 = -h0        # W układzie NED: Z = -h

h_flight = 2.0  # Wysokość lotu nad terenem [m]
Vel = 1.0       # Prędkość referencyjna [m/s]

# ============================================================================
# PHYSICAL LIMITS
# ============================================================================

T_max = 400.0
T_min = 0.0

# ============================================================================
# SANITY CHECKS
# ============================================================================

if __name__ == "__main__":
    print("=== DRONE PARAMETERS (NED) ===")
    print(f"Mass: {MASS} kg")
    print(f"Motor arm length: {MOTOR_ARM_LENGTH} m")
    print(f"Moment of inertia Iy: {IY:.4f} kg·m²")
    print(f"Weight: {MASS * g:.2f} N")
    print(f"Thrust per motor (hover): {MASS * g / 2:.2f} N")
    print(f"Max total thrust: {T_max * 2:.2f} N")
    print(f"Thrust-to-weight ratio: {T_max * 2 / (MASS * g):.2f}")
    print(f"Initial altitude h0: {h0} m")
    print(f"Initial Z position: {z0} m (NED: negative = up)")
